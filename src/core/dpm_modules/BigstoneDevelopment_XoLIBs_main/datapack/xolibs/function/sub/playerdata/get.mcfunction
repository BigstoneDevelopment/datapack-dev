# gets the player's data from the specified UUID's permanant storage

data remove storage bigstone_sandbox:playerdata storage
$data modify storage bigstone_sandbox:playerdata storage set from storage bigstone_sandbox:playerdata rom.$(UUID0)_$(UUID1)_$(UUID2)_$(UUID3)